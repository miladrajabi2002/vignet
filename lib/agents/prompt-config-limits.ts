/** Maximum rules accepted in each side of the six-layer prompt scope (200 total). */
export const PROMPT_SCOPE_RULE_LIMIT = 100

/** Maximum length of one must-do / must-not rule. */
export const PROMPT_SCOPE_RULE_MAX_CHARS = 500
