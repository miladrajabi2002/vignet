/** Maximum rules accepted in each side of the six-layer prompt scope. */
export const PROMPT_SCOPE_RULE_LIMIT = 60

/** Maximum length of one must-do / must-not rule. */
export const PROMPT_SCOPE_RULE_MAX_CHARS = 500
