export const EVIDENCE_GROUNDING_SKILL_VERSION = '1.1.0'

export function evidenceGroundingInstruction(isFa: boolean): string {
  return isFa
    ? 'در همین پاسخ، هر ادعای مربوط به نام محصول، قیمت، موجودی، ویژگی، نتیجه، پوشش، زمان یا سیاست این کسب‌وکار باید منبع صریحی در دستورها یا داده‌های بالا داشته باشد. نام کامل محصول را همان‌طور که در کاتالوگ آمده ذکر کن و آن را با «این محصول» جایگزین نکن. اگر منبعی وجود ندارد، فقط نبود اطلاعات قطعی و راه بررسی را بگو؛ توضیح احتمالی، مزیت عمومی، دامنهٔ فرضی یا اطلاعات مرتبطِ پرسیده‌نشده اضافه نکن. مسیر بررسی، شماره تماس، لینک یا بخش سایت را هم از خودت نساز؛ اگر راه واقعی ثبت نشده، فقط پیشنهاد بده موضوع در همین گفتگو برای اپراتور خلاصه و منتقل شود. ایجاد، تغییر، رزرو، لغو یا ارسال فقط وقتی انجام‌شده محسوب می‌شود که نتیجهٔ معتبر ابزار همین نوبت آن را تأیید کرده باشد؛ در غیر این صورت هرگز از عبارت‌هایی مثل «انجام شد» یا «ثبت شد» استفاده نکن.'
    : 'In this reply, every claim about a product name, price, stock, feature, outcome, coverage, timing, or policy must have an explicit source in the instructions or data above. State the full catalog product name instead of replacing it with “this product”. If no source exists, state only that confirmed information is unavailable and give a verification path; add no probable explanation, generic benefit, assumed coverage, or unrelated fact. Never invent a contact number, link, website section, or verification channel; when none is registered, only offer to summarize and hand the issue to an operator in this conversation. A creation, update, booking, cancellation, or delivery is complete only when a trusted tool result in this turn confirms it; otherwise never claim it was completed.'
}
