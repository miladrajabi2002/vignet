# Vignet Production Audit Report
**Date:** 2026-09-11
**Server:** 5.75.192.147 (`/var/www/vigent.ir/public/vignet/`)
**Project type:** Next.js 15 + Prisma 6 + next-intl + next-auth + BullMQ worker
**Total project size:** ~5.2 GB (incl. node_modules, builds, git)
**Source files:** 840 (.ts/.tsx/.js/.jsx/.mjs/.cjs)
**Tests:** 133 files
**API routes:** 122
**Migrations:** 54

---

## A. Build artifacts / caches (huge wins, zero risk)

These are gitignored and regenerated on every build — safe to remove without touching source.

| # | Path | Size | Why redundant | Confidence | Risk | Suggestion |
|---|------|------|---------------|-----------|------|-----------|
| 1 | `.next-builds/14444da0101e78e4bb7033486885e7483952eebe-1789092248-1742186/` | ~771 MB | Old release build (commit `14444da01`). Two newer builds of `ee3fe4dcf` exist (Sep 11 05:38 & 05:45). Used only for rollback — but the very next subdirectory below is the same commit, just newer. | High | Low (only loses rollback to commit `14444da01` which was 3 deploys ago) | DELETE (keep at most 1 previous + current) |
| 2 | `.next-builds/ee3fe4dcf6d848b547a87fc94d3a165ea999f589-1789092438-1747828/` | ~766 MB | Older release build of `ee3fe4dcf` from 05:38; the 05:45 build supersedes it. | High | Low (loses 1 rollback point for same commit) | DELETE (keep latest `ee3fe4dcf...-1789092778-1754560` for current rollback) |
| 3 | `.next/` | ~1.3 GB | Current build dir (`.next`). Heavily referenced by `next.config.mjs` `distDir`. **Do NOT delete** — only relevant if you want to wipe and rebuild. | — | — | KEEP (in-use) |
| 4 | `graphify-out/2026-07-15/` … `2026-08-11/` (16 daily snapshot dirs) | ~83 MB | Historical graphify code-graph snapshots — dev-only, never imported. Latest is `2026-08-25/`. Only the most recent snapshot is useful for `graphify query`. | High | Low | DELETE the 15 older snapshots, KEEP only `2026-08-25/`, `cache/`, `graph.json`, `graph.html`, `manifest.json`, `GRAPH_REPORT.md`, `memory/`, `reflections/` |
| 5 | `graphify-out/` as a whole | 89 MB | Even though `AGENTS.md` references it, this is purely a *dev-agent* knowledge graph, not used by the running app. The whole dir is gitignored already? No — `.gitignore` does NOT list it. | Medium | Medium (some devs rely on `graphify query` for codebase navigation) | REVIEW — at minimum, archive old daily snapshots; consider `git rm -r --cached graphify-out/` and add to `.gitignore` |
| 6 | `tsconfig.tsbuildinfo` | 663 KB | TypeScript incremental build cache. Gitignored (`*.tsbuildinfo` in `.gitignore`). Regenerated on next `tsc`. | High | None | DELETE |
| 7 | `next-env.d.ts` | 329 B | Auto-generated by Next.js. Gitignored. | High | None | KEEP (Next.js regenerates on dev/build) |

**Subtotal reducible:** ~1.6 GB (build dirs + old graphify snapshots + tsbuildinfo)

---

## B. Dead / unused source code

| # | Path | Why unused | Confidence | Risk | Suggestion |
|---|------|-----------|-----------|------|-----------|
| 1 | `app/api/admin/blog/categories/route.ts` | Exports `GET` + `POST` for `/api/admin/blog/categories`. Searched all components and `app/`: zero callers. `components/blog/admin-blog-manager.tsx` calls `/api/admin/blog/posts*` and `/api/admin/blog/upload`, but never `/categories`. The categories feature appears to have been replaced by free-text tags or removed. | High | Low (no UI calls it; if an external admin tool used it, would show in access logs — worth grepping nginx logs first) | DELETE |
| 2 | `cleanup-e2e-test-data.js` (root) | 807-byte one-off CommonJS script that deletes test data from a hard-coded workspace `cmqxoy39y0000eoapw2pt9yat`. No references anywhere. Belongs in `scripts/` if kept at all. | High | Low (only deletes test data, no prod impact) | DELETE (or move to `scripts/` if still needed) |
| 3 | `lib/maintenance/data-retention.ts` — exports `AUDIT_RETENTION_DAYS`, `STORE_SYNC_SUCCESS_RETENTION_DAYS`, `STORE_SYNC_ERROR_RETENTION_DAYS`, `ORPHAN_WORKSPACE_GRACE_DAYS`, `CleanupResult` interface | These 5 symbols are exported but only used internally within the same file (lines 25–64 of `cleanupOldRecords`). No external importer. Removing the `export` keyword (turning them into module-private) is safe. | High | None | REVIEW — drop `export` from these 5 symbols (keep `cleanupOldRecords` exported, it's used by worker + test) |
| 4 | `components/marketing/home-sections.tsx`, `components/marketing/home-variants/shared/get-variant-props.ts`, `components/marketing/home-variants/shared/types.ts`, `components/marketing/motion-provider.tsx`, `components/marketing/vigento-section.tsx`, `components/admin/mini-trend.tsx` | Flagged by initial pass — need manual verification. `home-variants/shared/` has `types.ts` and `get-variant-props.ts` that look like scaffolding for A/B test variants that may have been superseded. | Low (need verification) | Medium | REVIEW |

---

## C. Unused environment variables (7 confirmed)

Searched `process.env.KEY` and bare `KEY` across all `.ts/.tsx/.js/.mjs/.sh/.sql` files.

| # | Env var | Why unused | Confidence | Risk | Suggestion |
|---|---------|-----------|-----------|------|-----------|
| 1 | `PLAN_LIMIT_TRIAL_MSGS` | Comment in `lib/billing/plans.ts:8` explicitly says: *"There is no plan-level message quota; availability is governed by subscription state and the workspace's reply-credit balance."* Code reads `PLAN_LIMIT_*_CHANNELS`, `_AGENTS`, `_PRODUCTS`, `_ORDERS`, `_CUSTOMERS` — but never `_MSGS`. | High | None (feature explicitly removed) | DELETE from `.env` and `.env.example` |
| 2 | `PLAN_LIMIT_STARTER_MSGS` | Same as above | High | None | DELETE |
| 3 | `PLAN_LIMIT_PRO_MSGS` | Same as above | High | None | DELETE |
| 4 | `PLAN_LIMIT_BUSINESS_MSGS` | Same as above | High | None | DELETE |
| 5 | `WHATSAPP_BRIDGE_HOST` | Comment in `.gitignore`: *"Retired WhatsApp bridge artifacts (may contain live session credentials) /mini-services/whatsapp-bridge/"*. The `mini-services/` dir doesn't exist anymore. Only references to `WHATSAPP_BRIDGE_*` are inside `graphify-out/` historical snapshots. | High | Low (WhatsApp bridge is retired; if you re-enable WhatsApp you'd re-add these) | DELETE from `.env` and `.env.example` |
| 6 | `WHATSAPP_BRIDGE_SECRET` | Same as above | High | Low | DELETE |
| 7 | `WHATSAPP_BRIDGE_URL` | Same as above | High | Low | DELETE |

---

## D. One-off / unreferenced scripts in `scripts/` (21 files)

These are *not* invoked from `package.json scripts:` nor from `deploy/`. Many are clearly one-off cleanups already executed against production. They aren't "broken" but they pollute the repo and mislead future contributors into thinking they're still relevant.

| # | Script | Type | Last modified | Confidence | Risk | Suggestion |
|---|--------|------|---------------|-----------|------|-----------|
| 1 | `agent-closing-smoke.ts` | smoke test | 2026-09-06 | Medium | Low | REVIEW — move to `scripts/smoke/` or delete if `agent-live-smoke.ts` supersedes it |
| 2 | `cleanup-automation-orphans.ts` | one-off DB cleanup | 2026-09-04 | High | None (already executed) | DELETE (or move to `scripts/oneoffs/`) |
| 3 | `cleanup-orphaned-markers.ts` | one-off DB cleanup | 2026-09-04 | High | None | DELETE (or archive) |
| 4 | `enforce-order-retention.mjs` | one-off enforcement | 2026-08-25 | High | None | DELETE (or archive) |
| 5 | `fetch_profile.ts` | debug util | 2026-09-04 | Low | Low | REVIEW |
| 6 | `improvement-smoke.ts` | smoke test | 2026-09-08 | Medium | Low | REVIEW |
| 7 | `indexnow-submit.py` | SEO ping utility | 2026-09-10 | Medium | Medium (might be invoked manually for SEO re-indexing after blog changes) | REVIEW — wire into `package.json` scripts if used periodically |
| 8 | `inspect-blog-content.cjs` | debug util | 2026-09-04 | High | None | DELETE |
| 9 | `migrate-business-types.sql` | one-off migration | 2026-07-12 | High | None (already applied — see migrations `20260626000000_init` etc.) | DELETE |
| 10 | `migrate-business-types.ts` | one-off migration | 2026-07-12 | High | None | DELETE |
| 11 | `purge-woo-dead.ts` | one-off cleanup | 2026-09-07 | High | None | DELETE (or archive) |
| 12 | `repair-automation-only-handoffs.ts` | one-off repair | 2026-09-06 | High | None | DELETE (or archive) |
| 13 | `repair-instagram-test-contact.ts` | one-off repair | 2026-09-06 | High | None | DELETE |
| 14 | `retry-woo-failed.ts` | one-off retry | 2026-09-07 | Medium | Low | REVIEW — keep if Woo sync retries are recurring |
| 15 | `seed-blog-seo.mjs` | one-off seeding | 2026-09-04 | High | None (blog posts already seeded) | DELETE (or archive) |
| 16 | `seed-demo.ts` | demo data seeding | 2026-08-03 | Medium | Low | REVIEW — keep if demo workspace is re-seeded periodically |
| 17 | `seo-articles-2026-09.mjs` | one-off SEO content generation | 2026-09-04 | High | None (articles already created) | DELETE (or archive to `scripts/seed/`) |
| 18 | `seo-case-study-2026-09.mjs` | one-off SEO case study | 2026-09-04 | High | None | DELETE (or archive) |
| 19 | `start-demo.sh` | demo launcher | 2026-07-12 | Low | Low | REVIEW |
| 20 | `vitrin-live-verify.ts` | live verification | 2026-09-11 | Medium | Low | REVIEW — recent (today); likely still in active use |
| 21 | `minify-widget.mjs` | build tooling | 2026-09-04 | Medium | Medium (produces `public/widget/loader.js` from `loader.src.js`) | KEEP — referenced by build flow even if not in package.json |

---

## E. Old / one-off files in `deploy/` (3 candidates)

| # | Path | Why old | Confidence | Risk | Suggestion |
|---|------|---------|-----------|------|-----------|
| 1 | `deploy/fix-businesstype-enum.sql` | One-off SQL to add `SOCIAL`/`SUPPORT` to `BusinessType` enum. Same purpose as `scripts/migrate-business-types.{sql,ts}`. Migration `20260626000000_init` already covers it. | High | None (already applied) | DELETE (or archive to `deploy/oneoffs/`) |
| 2 | `deploy/purge-inactive-users-2026-09-11.sql` | Dated one-off purge of 15 trial users. Comment: *"Run: sudo -u postgres psql -d vigent -f /tmp/purge_inactive_users.sql"*. Matches git log entry `f6ea3090e chore(ops): purge 15 inactive trial users (>20d idle) with all data` from yesterday. Already executed. | High | None (already executed) | DELETE (or archive) |
| 3 | `deploy/setup-server.sh` | Initial server provisioning script. Useful when re-provisioning. | Medium | Low | KEEP (initial provisioning reference) |

---

## F. Root-level markdown / docs (3 candidates)

| # | File | Size | Last modified | Why unused | Confidence | Risk | Suggestion |
|---|------|------|---------------|-----------|-----------|------|-----------|
| 1 | `vigent-prompt-v2.md` | 54 KB | 2026-08-08 | Zero references in code. Title: *"VIGENT — Complete Claude Code Project Prompt v2"*. Looks like the original build prompt — superseded by the actual codebase. | High | Low (historical reference) | DELETE (or move to `docs/legacy/`) |
| 2 | `PRICING-GUIDE.fa.md` | 8.8 KB | 2026-07-26 | Only referenced from `CHANGES.md` (line 1 mentions it). Pricing logic lives in `lib/billing/plans.ts` and `lib/platform/commercial-config.ts`. | High | Low (Pricing info might be wrong/outdated anyway) | DELETE (or move to `docs/legacy/`) |
| 3 | `CHANGES.md` | 78 KB | 2026-09-09 | Referenced by `app/api/wordpress-plugin/info/route.ts` to serve changelog to WordPress plugin consumers. **Not actually dead** — exposed via API. | Low | Medium (breaking API change for plugin users) | KEEP — but consider truncating to last N entries |
| 4 | `AGENTS.md` | 1.1 KB | 2026-07-15 | Developer instructions for AI agents (graphify usage). Zero code references. | High | None (agent instructions only) | KEEP (useful for dev agents like me) |
| 5 | `README.md` | 7 KB | 2026-09-06 | Standard project README. | High | None | KEEP |

---

## G. `wordpress-plugin/` (13 files)

Confirmed IN-USE: referenced from `scripts/build-plugin-zip.mjs`, `components/products/woo-setup-card.tsx`, `components/onboarding/woo-connect-wizard.tsx`, and `CHANGES.md`. The packaged zip is published at `public/downloads/vigent-wordpress.zip` (460 KB).

**Suggestion:** KEEP — this is a real, distributed integration plugin.

One sub-note: `public/downloads/vigent-wordpress.zip` is a build artifact — should be regenerated by `npm run plugin:zip` and gitignored, not committed. **REVIEW** whether to gitignore it.

---

## H. `tests/` directory

**Total tests:** 133 files
**Tests with broken imports:** 0 (after fixing regex bug — all imports resolve to real symbols)
**Tests with `legacy/old/removed` markers in filename:** 1 (`tests/docs-removed-pages.test.ts`) — but this is actually a *removal-guard test* that asserts deprecated pages stay unpublished. **KEEP**.

### Tests with "legacy" markers in content (19 files)

These contain words like "removed/legacy/deprecated/migrated/old" — but in **all cases inspected**, the markers are inside comments explaining *why* a behavior changed (e.g. "previously we used X, now we use Y; ensure X stays removed"). These are **valuable regression tests** and must NOT be deleted just because they mention legacy behavior.

Examples:
- `tests/docs-removed-pages.test.ts` — asserts removed docs stay removed ✅ KEEP
- `tests/phone-identity.test.ts` — tests current phone identity logic with comments about migration history ✅ KEEP
- `tests/conversation-session.test.ts` — same pattern ✅ KEEP
- `tests/wordpress-plugin-sync-durability.test.ts` — tests current plugin sync, comment notes legacy sync was less durable ✅ KEEP

### Conclusion: zero tests are confidently deletable

The test suite is clean. **No tests should be deleted based on this audit.** All 133 tests either:
- Cover current production behavior, OR
- Act as regression guards for removed features (valuable "stay-removed" tests)

---

## I. Duplicated component file names (3 pairs, all FALSE POSITIVES)

| Pair | Status |
|------|--------|
| `components/dashboard/bar-list.tsx` vs `components/dashboard/charts/bar-list.tsx` | Both used. Different sizes (1.4 KB vs 0.9 KB). Different content. |
| `components/instagram/voice-recorder.tsx` vs `components/voice/voice-recorder.tsx` | Both used. Different sizes (10 KB vs 4 KB). Different components — instagram one handles IG-specific flow. |
| `components/instagram/types.ts` vs `components/marketing/home-variants/shared/types.ts` | Both used. Different domains. |

**Suggestion:** KEEP — they're not duplicates, just same-named files in different namespaces. (Optional: rename for clarity.)

---

## J. Dependencies — none confidently removable

| Package | Used in | Verdict |
|---------|---------|---------|
| All 21 runtime deps | Multiple source files | KEEP — all used |
| All 14 devDeps | Build/test tooling | KEEP — `@types/*`, `eslint*`, `postcss`, `tailwindcss`, `tsx`, `typescript`, `vitest`, `dotenv`, `terser`, `prisma` all invoked by `npm run` scripts |
| `@swc/helpers` | Implicit SWC dependency | KEEP (transitive build dep) |

**Conclusion: 0 dependencies removable.** The `package.json` is tight.

---

## K. CSS / Tailwind

- `tailwind.config.ts` (2.5 KB) — active, scans `app/**`, `components/**`, `lib/**`. KEEP.
- `postcss.config.mjs` (135 B) — references `tailwindcss` and `autoprefixer`. KEEP.
- `app/globals.css` — global stylesheet, KEEP.
- No stray `.css` files outside `app/` found.

**Conclusion: nothing to remove.**

---

## L. Cron / worker / webhook inventory

### Cron (root crontab)
- `0 3 * * *` — `bash deploy/backup.sh` → nightly backup. KEEP.
- (Other crons belong to unrelated projects on the same server — not Vigent's concern.)

### PM2 processes (related to Vigent)
- `vignet-web` — Next.js production server (port 3003) ✅
- `vignet-worker` — BullMQ worker (`tsx worker/index.ts`) ✅
- `vignet-studio` — Prisma Studio (port 5555, internal-only) ✅ — used for admin DB inspection
- All three are active and required.

### Webhook routes (all externally-called, KEEP)
- `app/api/webhook/{bale,instagram,resend,rubika,telegram}/route.ts`
- `app/api/telegram-operator/webhook/route.ts`
- `app/api/billing/callback/{zarinpay,nowpayments}/route.ts`
- `app/api/auth/[...nextauth]/route.ts`

### Worker jobs (`worker/scheduler.ts` 868 lines)
Job names are inline strings in `scheduler.ts`. The worker runs `cleanupOldRecords`, trial lifecycle sweep (`runTrialLifecycleSweep`), and other periodic tasks. All wired to `lib/queue/*`. KEEP.

---

## M. Other (types, i18n, fonts, etc.)

| Path | Status |
|------|--------|
| `types/next-auth.d.ts` | Module augmentation for next-auth — referenced by `auth.ts`. KEEP. |
| `i18n/request.ts` | next-intl request config. KEEP. |
| `messages/en.json`, `messages/fa.json` | Locale catalogs (92 KB each). Referenced by `next-intl`. KEEP. |
| `public/fonts/IRANSansWeb*.woff2` (4 files, ~120 KB) | Referenced in `app/layout.tsx` via `localFont`. KEEP. |
| `public/brands/{bale,rubika}-logo.svg` | Used in marketing + channel setup UIs. KEEP. |
| `public/widget/loader.src.js` (151 KB) | Source for `loader.js`; consumed by `scripts/minify-widget.mjs`. KEEP. |
| `public/widget/loader.js` (57 KB) | Minified widget loader, served at `/widget/loader.js`. KEEP. |
| `public/downloads/vigent-wordpress.zip` (460 KB) | Built artifact; consider gitignoring. REVIEW. |
| `prisma/migrations/` (54 migrations) | Migration history — **DO NOT DELETE**. Prisma best practice is to keep all migrations for reproducibility. |
| `.codex/hooks.json` | Codex CLI hooks config. KEEP. |
| `.agents/skills/*` (14 dev-tool files) | AI agent skill files. KEEP. |
| `skills-lock.json` | Skill version lock. KEEP. |
| `deploy/ippanel-proxy/` (PHP files) | Source code of the OTP proxy deployed at `jetstars.ir`. Referenced from `.env` `IPPANEL_PROXY_URL`. KEEP. |

---

## N. Summary

### Numbers

| Category | Count |
|----------|-------|
| Source files confidently deletable | **3** (`app/api/admin/blog/categories/route.ts`, `cleanup-e2e-test-data.js`, see §B) |
| Source files needing REVIEW | **6** (marketing home-variants scaffolding, see §B #4) |
| Build artifacts / caches deletable | **5** (2 old `.next-builds/` dirs, 15 old `graphify-out/` snapshots, `tsconfig.tsbuildinfo`, see §A) |
| Tests deletable | **0** |
| Dependencies deletable | **0** |
| Env vars deletable | **7** (4 `PLAN_LIMIT_*_MSGS` + 3 `WHATSAPP_BRIDGE_*`, see §C) |
| One-off scripts deletable / archivable | **~14 of 21** (see §D — confirmed one-offs) |
| Old deploy SQL files deletable | **2** (`fix-businesstype-enum.sql`, `purge-inactive-users-2026-09-11.sql`, see §E) |
| Root markdown files deletable | **2** (`vigent-prompt-v2.md`, `PRICING-GUIDE.fa.md`, see §F) |

### Disk space reduction estimate

| Item | Size |
|------|------|
| `.next-builds/` (delete 2 of 3 dirs) | **~1.5 GB** |
| `graphify-out/` old daily snapshots (15 dirs) | **~80 MB** |
| `tsconfig.tsbuildinfo` | **663 KB** |
| `vigent-prompt-v2.md` + `PRICING-GUIDE.fa.md` | **63 KB** |
| 14 one-off scripts | **~80 KB** |
| Dead API route + root .js file | **~3 KB** |
| Old deploy SQL | **~9 KB** |
| **Total** | **~1.58 GB** (almost entirely from build artifacts) |

### Most important dead code findings
1. **`app/api/admin/blog/categories/route.ts`** — entire API endpoint with no frontend caller (categories feature appears removed)
2. **7 unused env vars** — clear "leftovers" from removed WhatsApp bridge + removed per-plan message quota
3. **`lib/maintenance/data-retention.ts`** — 5 exports used only internally; should be unexported
4. **14 one-off scripts** cluttering `scripts/` (DB cleanups, seedings, migrations already applied)
5. **2 old release build dirs** in `.next-builds/` (~1.5 GB of rollback history nobody asked for)

### High-risk deletions (DO NOT auto-delete)
- `CHANGES.md` — exposed via `/api/wordpress-plugin/info` route (would break plugin update notification flow)
- `graphify-out/2026-08-25/` (latest) + `graph.json` + `manifest.json` — used by `graphify query` dev workflow
- Any `prisma/migrations/*` — required for migration history
- All webhook routes (`/api/webhook/*`) — externally called by Meta/Telegram/Resend/etc.
- All `tests/*` — no test is confidently dead
- `deploy/ippanel-proxy/` — source for the deployed OTP proxy at `jetstars.ir`

### Pre-deletion checklist (when you say "پاکسازی را اجرا کن")
1. Back up `.env` before removing the 7 vars.
2. Grep nginx access logs for `/api/admin/blog/categories` to confirm zero external traffic before deleting the route.
3. Before removing old `.next-builds/` dirs, confirm `pm2 info vignet-web | grep "script path"` is using the latest build.
4. Run `npm run lint && npx tsc --noEmit && npm run build && npm test` after each batch of deletions.
5. Restart `pm2 restart vignet-web vignet-worker` after env changes.

---

*End of audit report.*
