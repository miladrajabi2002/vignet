import { AutomationFormSkeleton } from '@/components/dashboard/pages-skeletons'

/**
 * Route-level skeleton for /instagram/[automationId]/edit — mirrors
 * AutomationForm (edit mode) with the same 2-column layout.
 */
export default function InstagramEditAutomationLoading() {
  return <AutomationFormSkeleton />
}
