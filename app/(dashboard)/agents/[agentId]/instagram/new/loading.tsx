import { AutomationFormSkeleton } from '@/components/dashboard/pages-skeletons'

/**
 * Route-level skeleton for /agents/[agentId]/instagram/new — mirrors the
 * AutomationForm (create mode) 2-column layout.
 */
export default function AgentInstagramNewLoading() {
  return <AutomationFormSkeleton />
}
