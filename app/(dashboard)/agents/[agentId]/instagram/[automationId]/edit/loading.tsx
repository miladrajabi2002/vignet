import { AutomationFormSkeleton } from '@/components/dashboard/pages-skeletons'

/**
 * Route-level skeleton for /agents/[agentId]/instagram/[automationId]/edit
 * — mirrors the AutomationForm (edit mode) 2-column layout.
 */
export default function AgentInstagramEditLoading() {
  return <AutomationFormSkeleton />
}
