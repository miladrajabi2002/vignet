import { OnboardingFlowSkeleton } from '@/components/dashboard/pages-skeletons'

/**
 * Route-level skeleton for /onboarding — mirrors the OnboardingFlow page:
 * a vertically centered card with the business-type tile grid and the
 * continue button.
 */
export default function OnboardingLoading() {
  return <OnboardingFlowSkeleton />
}
