# dependencies
node_modules/

# baileys auth state — one folder per connected WhatsApp account.
# These contain the cryptographic credentials tied to each phone number;
# never commit them. Back them up off-site instead.
auth/

# local env
.env

# logs
*.log

# os / editor cruft
.DS_Store
.idea/
.vscode/
